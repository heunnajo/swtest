해시 함수를 이용해서 문자열을 비교한다.
해시 함수 구현에 따라 시간복잡도가 차이난다.
자주 사용하는 해시 함수는 라빈 핑거프린트이다.
라빈 핑거프린트는 문장열을 구성하는 문자의 집합을 알고 있을 때 사용한다.
ex) 문자의 아스키 값을 이용하고, 256진법을 이용하고, 소수는 127을 이용한 예시
"abc"의 라빈 핑거프린트는 다음과 같이 계산할 수 잉ㅆ다.
(97*256^2 + 98*256+99) % 127
= ((((97*256)%127 + 98)%127*256)%127+106)%127
